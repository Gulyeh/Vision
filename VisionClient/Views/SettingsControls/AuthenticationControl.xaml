<UserControl x:Class="VisionClient.Views.SettingsControls.AuthenticationControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VisionClient.Views.SettingsControls"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="#0c3c60">
    <UserControl.Resources>
        <Style TargetType="TextBox">
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="Padding" Value="5,8,0,0"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Width" Value="100"/>
            <Setter Property="Background" Value="#0c3c60"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
        <Style TargetType="Button" x:Key="AppButton">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <ContentPresenter/>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.8"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="Button" x:Key="ButtonTemplate">
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="Width" Value="70"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Background" Value="#0AA1DD"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#0f3452"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Name="Border" Background="{TemplateBinding Background}">
                            <ContentPresenter VerticalAlignment="Center"
                                          HorizontalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid>
        <ScrollViewer HorizontalScrollBarVisibility="Disabled" 
                      VerticalScrollBarVisibility="Hidden"
                      CanContentScroll="False" Margin="0,0,0,20">
            <StackPanel Orientation="Vertical" 
                        HorizontalAlignment="Stretch" 
                        Margin="50,20,20,0">
                <TextBlock Text="Two-Factor Authentication" FontSize="20" Foreground="White" Margin="0,0,0,10"/>
                <Separator Margin="0,0,0,20" Height="0.9"/>
                <Border Width="600" Height="250" CornerRadius="2"
                        HorizontalAlignment="Left"
                        Background="#0f3452">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <Image Source="https://pl.online-qrcode-generator.com/QR-Code/qrcode-generator.png" Height="150"/>
                        <StackPanel Orientation="Vertical" 
                                    Margin="10,0,0,0"
                                    VerticalAlignment="Center">
                            <TextBlock Text="Security Code" FontSize="12" 
                                   Foreground="Gray"
                                   DockPanel.Dock="Top"
                                   Margin="0,0,0,5"/>
                            <TextBox Text="{Binding SecurityCode, Mode=TwoWay}"/>
                            <Button Content="Save"
                                    Margin="0,30,0,0"
                                    Style="{StaticResource ButtonTemplate}"
                                    Command="{Binding EnableAuthCommand}"
                                    FontSize="15"
                                    Width="150"
                                    Height="40"/>
                        </StackPanel>
                        <StackPanel Orientation="Vertical"
                                    VerticalAlignment="Center"
                                    Margin="80,0,0,0">
                            <Button VerticalAlignment="Center"
                                    Background="Transparent"
                                    BorderThickness="0"
                                    Margin="0,0,0,30"
                                    Style="{StaticResource AppButton}"
                                    Command="{Binding GetAppCommand}"
                                    CommandParameter="Google">
                                <Image Source="https://upload.wikimedia.org/wikipedia/commons/thumb/7/78/Google_Play_Store_badge_EN.svg/640px-Google_Play_Store_badge_EN.svg.png"
                                       Height="40"/>
                            </Button>
                            <Button VerticalAlignment="Center"
                                    Background="Transparent"
                                    BorderThickness="0"
                                    Style="{StaticResource AppButton}"
                                    Command="{Binding GetAppCommand}"
                                    CommandParameter="Apple">
                                <Image Source="https://upload.wikimedia.org/wikipedia/commons/thumb/3/3c/Download_on_the_App_Store_Badge.svg/2560px-Download_on_the_App_Store_Badge.svg.png"
                                       Height="40"/>
                            </Button>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
